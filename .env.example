# Docker Hub credentials for release.sh
DOCKERHUB_USERNAME=your-dockerhub-username
DOCKERHUB_TOKEN=your-dockerhub-token

# Optional overrides
DOCKERHUB_REPOSITORY=eventboxsentiment
IMAGE_TAG=latest
DOCKERFILE=Dockerfile
CONTEXT=.
