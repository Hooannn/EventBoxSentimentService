# Core API
fastapi
uvicorn
pydantic

# ML Inference (CHỈ dùng PyTorch - bỏ TensorFlow)
torch
transformers

# Text processing (bỏ gensim nếu không cần)
underthesea

# Optional: scikit-learn chỉ nếu thực sự cần
# scikit-learn==1.3.2
